define ['core'], ->

  # Imports
  # =============================================================

  Core = require 'core'

  # MontadoraModel class
  # =============================================================

  class MontadoraModel extends Core.Model

    urlRoot: '/api/montadora'
